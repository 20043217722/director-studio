---
name: dreambox
description: >-
  造梦空间 AI 电影管线 v2.1。完整 AIGC 视频 0-1 生产：前期策划→角色定妆→场景定妆→关键帧→视频→补镜头→声音→后期检查。
  含 7 专业智能体、角色一致性锁定、模型降级重试、vision 审核(L1自动+L2可选)。
  触发词：拍短片、做短剧、AI视频、AIGC创作、分镜脚本。
---

# Dreambox v2.1

## 文件地图

| 模块 | 文件 |
|------|------|
| **8 智能体** | `agents/*.md` |
| 导演 | `agents/director.md` (剧本+分镜+资产清单) |
| 视觉解析师 | `agents/visual-analyst.md` (图片色彩影调提取) |
| 人物造型 | `agents/character.md` (角色视觉,中英双语) |
| 场景设计 | `agents/scene.md` |
| 美术指导 | `agents/art-director.md` (视觉DNA) |
| 摄影指导 | `agents/cinematography.md` (关键帧,静帧) |
| 剧幕文戏 | `agents/motion.md` (视频运动) |
| 声音设计 | `agents/sound.md` |
| **执行工具** | `scripts/*.py` |
| 批量执行(v2) | `scripts/batch-run.py` (模型降级+重试退避) |
| 管线创建 | `scripts/pipeline.py` (一键建画布) |
| 导演输出解析 | `scripts/parse-director-output.py` |
| Vision审核 | `scripts/vision-review.py` (L1自动+L2可选) |
| **文档** | `docs/*.md` |
| 0-1生产阶段 | `docs/production-stages.md` (8阶段对照) |
| 角色一致性 | `docs/character-consistency.md` (面锚模板+锁定) |
| 模型选型 | `docs/model-guide.md` |
| 模型参数 | `docs/model-params.md` (quality/ratio/duration) |
| 提示词检查 | `docs/prompt-checklist.md` |
| 多镜批量 | `docs/multi-shot.md` |
| 迭代优化 | `docs/iteration.md` |
| 测试发现 | `docs/test-findings.md` (实地测试记录) |
| **Midjourney适配** | `docs/midjourney-guide.md` (MJ提示词铁律与转换指南) |
| **预设** | `presets/*.md` |
| 真人短剧 | `presets/realistic-drama.md` |
| 动画 | `presets/animation.md` |
| 广告TVC | `presets/tvc-ad.md` |

## 0-1 生产流程

```
[阶段0] 前期策划 -> 导演出剧本/分镜 -> (可选)视觉解析师分析参考图 -> 建画布节点
[阶段1] 角色定妆 -> 2-3模型各出1张 -> 审核锁定 -> --left引用
[阶段2] 场景定妆 -> 同上
[阶段3] 关键帧   -> 摄影指导逐镜(引用角色+场景) -> batch-run --stage 3
[阶段4] 视频     -> 剧幕文戏逐镜(引用关键帧) -> batch-run --stage 4
[阶段5] 补镜头   -> 对比分镜查漏 -> 针对性重出
[阶段6] 声音     -> 声音方案 -> --run
[阶段7] 后期检查 -> 分辨率/宽高比/一致性/光影/色彩/音频
```

## 模型降级链

图片: Lib Image -> Seedream 5.0 Pro -> Lib Navo Pro -> Seedream 5.0 Lite
视频: Seedance 1.0 Pro -> Hailuo 2.3 -> Kling 3.0

batch-run.py 遇到算力不足(1200000136)时自动沿降级链切换模型。

## 角色一致性

核心: 先出定妆照 -> 锁定为 --left -> 所有镜头引用同一参考图。
面锚逐字复用，不可改一字。

详见 docs/character-consistency.md

## Vision 审核

L1(自动): 元数据检查 - 生成状态/URL/尺寸, 无需API
L2(可选): vision.py 内容评估 - 需配置 API key

详见 docs/test-findings.md


## 会话稳定性协议（长时使用必读）

长时间使用造梦空间智能体后可能出现内容生成一半停止、卡顿、不稳定的问题。根本原因是：
- 上下文窗口被历史对话和巨型输出填满
- 模型注意力在超长输出中逐渐衰减
- 复杂嵌套指令在长会话中相互冲突

以下协议所有智能体必须遵守：

### 输出分段与检查点
- 每个智能体输出超过 800 字时必须分段输出，每段结束输出检查点标记
- 检查点格式: >>> [检查点] 阶段名 已完成 | 下一阶段: XXX | 进度 N/M
- 如果输出在中途截断，用户回复 继续，智能体必须从最后一个检查点开始续写

### 续写协议（强制）
- 收到 继续 指令时，智能体必须回读上一段输出的最后 100 字，找到最后一个 [检查点] 标记
- 从该检查点之后的内容续写，不重复已输出的内容
- 续写前输出: >>> [续写] 从检查点「XXX」继续...

### Token预算意识
- 每个智能体在生成超长输出前，先估算本次输出大约需要多少内容
- 如果预估超过 1500 字，主动提示用户: 本次输出较长（约N字），可能分批生成，请回复「继续」推进

### 智能体状态保持
- 跨会话时，用户需要引用上一会话最后生成的提示词作为输入
- 每个智能体输出末尾附带一条 [状态快照] 行，简要列出本次已完成的关键决策
- 下次继续时，用户将状态快照粘贴给智能体即可恢复上下文

---

## 📑 会话导航系统（快速定位历史输出）

长时间创作后，往上翻找之前的提示词非常耗时。所有智能体的输出最前面必须包含一个「会话导航」块，列出本会话中所有的输出记录和快速描述。

### 导航格式（强制·每个智能体输出开头必须包含）

`
## 📑 会话导航

| # | 阶段 | 内容摘要 | 生成时间 |
|---|------|------|------|
| 1 | 导演 | 剧本:《贺准的出狱》·5镜分镜 | 14:22 |
| 2 | 美术指导 | 全片视觉世界观·冷蓝灰/暖褐双色系 | 14:25 |
| 3 | 视觉解析师 | 参考图分析·色板提取·蓝灰冷低饱\|中调高反差硬 | 14:28 |
| 4 | 场景设计 | 监狱铁门内侧·夜21:47·中雨·锈蚀铁门 | 14:32 |
| 5 | 摄影指导 | 镜1-3关键帧·MCU→CU·冷暖光分割 | 14:38 |
| ... | ... | ... | ... |

> 往上滑动可以按编号快速定位之前的输出内容
`

### 导航维护规则
- 智能体每次输出时，在上一次导航表的基础上新增一行
- 如果当前智能体是第一次输出，创建新导航表
- 内容摘要 ≤30 字，用白话概述本次输出了什么
- 导航表放在输出最前面，代码块之前
- 如果会话重新开始，导航表也重新从第 1 行开始

---
## 已知限制

- libtv 高峰期可能算力不足(1200000136), batch-run.py 已内置降级重试
- 不同模型 quality 参数值不同, 见 docs/model-params.md
- prompt 中的 --ar 不被解析, 必须用 -s ratio=X:Y
- 生成结果审核仍需人工最终判断(尤其角色一致性)

## 前置依赖

- libtv CLI 已登录
- (可选) DOUBAO_API_KEY/DASHSCOPE_API_KEY/OPENAI_API_KEY 用于 L2 vision
